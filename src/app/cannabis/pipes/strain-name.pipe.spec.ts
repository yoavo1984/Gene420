import { StrainNamePipe } from './strain-name.pipe';

describe('StrainNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StrainNamePipe();
    expect(pipe).toBeTruthy();
  });
});
