import { StrainTypePipe } from './strain-type.pipe';

describe('StrainTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StrainTypePipe();
    expect(pipe).toBeTruthy();
  });
});
